<Window x:Class="Photobooth.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Photobooth"
        mc:Ignorable="d"
        Title="Photobooth" 
        Height="1080" Width="1920"
        WindowState="Maximized" 
        WindowStyle="None"
        Background="Black"
        >
    <!-- Main Container for all screens -->
    <Grid Name="MainContainer">
        <!-- Content will be dynamically loaded here -->
        <ContentControl Name="CurrentScreenContainer" Panel.ZIndex="0" />

        <!-- Modern Notification Container (top-right) -->
        <Grid Name="NotificationContainer" 
              HorizontalAlignment="Right" 
              VerticalAlignment="Top"
              Panel.ZIndex="10000"
              IsHitTestVisible="True">
            <!-- Notifications will be added here dynamically -->
        </Grid>

        <!-- Invisible Admin Access Zone (top-left corner) - HIGH PRIORITY -->
        <Border Name="AdminAccessZone"
                HorizontalAlignment="Left" 
                VerticalAlignment="Top"
                Width="150" 
                Height="150"
                Background="Transparent"
                MouseLeftButtonDown="AdminAccessZone_MouseLeftButtonDown"
                Cursor="Hand"
                Panel.ZIndex="999"
                Margin="0,0,0,0">
            <!-- Debug indicator (slightly more visible for testing) -->
            <TextBlock Text="👤" 
                       FontSize="20" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center" 
                       Foreground="White" 
                       Opacity="0.2"/>
        </Border>
    </Grid>
</Window>
