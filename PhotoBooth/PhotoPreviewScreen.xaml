<UserControl x:Class="Photobooth.PhotoPreviewScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920">
    
    <UserControl.Resources>
        <!-- Main Background Gradient: from-purple-900 via-blue-900 to-indigo-900 -->
        <LinearGradientBrush x:Key="MainBackgroundGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#581C87" Offset="0"/>
            <!-- purple-900 -->
            <GradientStop Color="#1E3A8A" Offset="0.5"/>
            <!-- blue-900 -->
            <GradientStop Color="#312E81" Offset="1"/>
            <!-- indigo-900 -->
        </LinearGradientBrush>

        <!-- Floating Orb Styles -->
        <Style x:Key="FloatingOrbStyle" TargetType="Ellipse">
            <Setter Property="Effect">
                <Setter.Value>
                    <BlurEffect Radius="15"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Back Button Style -->
        <Style x:Key="BackButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                Padding="12">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <SolidColorBrush Color="White" Opacity="0.1"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Action Button Styles -->
        <Style x:Key="RetakeButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#6B7280"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="32,16"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="12" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4B5563"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#374151"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LoveButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#10B981"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="32,16"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="12" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#059669"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#047857"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <!-- Main Container -->
    <Grid Background="{StaticResource MainBackgroundGradient}" ClipToBounds="True">

        <!-- Floating Orbs Layer -->
        <Canvas Name="FloatingOrbsCanvas">
            <!-- Purple orb: top-32 left-32 w-24 h-24 bg-purple-500/20 -->
            <Ellipse Width="96" Height="96" 
                     Fill="#A855F7" Opacity="0.2" 
                     Style="{StaticResource FloatingOrbStyle}"
                     Canvas.Left="128" Canvas.Top="128"/>

            <!-- Blue orb: top-20 right-40 w-20 h-20 bg-blue-500/25 -->
            <Ellipse Width="80" Height="80" 
                     Fill="#3B82F6" Opacity="0.25" 
                     Style="{StaticResource FloatingOrbStyle}"
                     Canvas.Left="1680" Canvas.Top="80"/>

            <!-- Pink orb: bottom-40 left-1/4 w-32 h-32 bg-pink-500/15 -->
            <Ellipse Width="128" Height="128" 
                     Fill="#EC4899" Opacity="0.15" 
                     Style="{StaticResource FloatingOrbStyle}"
                     Canvas.Left="480" Canvas.Top="792"/>

            <!-- Cyan orb: bottom-32 right-32 w-20 h-20 bg-cyan-500/20 -->
            <Ellipse Width="80" Height="80" 
                     Fill="#06B6D4" Opacity="0.2" 
                     Style="{StaticResource FloatingOrbStyle}"
                     Canvas.Left="1712" Canvas.Top="872"/>
        </Canvas>

        <!-- Floating Particles Canvas -->
        <Canvas Name="ParticlesCanvas" ClipToBounds="True">
            <!-- Particles will be added programmatically -->
        </Canvas>

        <!-- Main Content Grid with Row Definitions -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Header -->
                <RowDefinition Height="*"/>
                <!-- Photo Display (takes most space) -->
                <RowDefinition Height="Auto"/>
                <!-- Template Info -->
                <RowDefinition Height="Auto"/>
                <!-- Action Buttons -->
                <RowDefinition Height="40"/>
                <!-- Bottom Spacing -->
            </Grid.RowDefinitions>

            <!-- Header Row - Same as Template Selection -->
            <Grid Grid.Row="0" Margin="30,30,30,20">
                <Button Name="BackButton"
                        Content="←"
                        Style="{StaticResource BackButtonStyle}"
                        HorizontalAlignment="Left"
                        Click="BackButton_Click"/>
                <!-- Styled gradient title -->
                <TextBlock Text="How do they look?" 
                           FontSize="36" 
                           FontWeight="Bold" 
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center">
                    <TextBlock.Foreground>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="#E0E7FF" Offset="0"/>
                            <!-- light indigo -->
                            <GradientStop Color="#DBEAFE" Offset="0.3"/>
                            <!-- light blue -->
                            <GradientStop Color="#ECFDF5" Offset="0.6"/>
                            <!-- light cyan/mint -->
                            <GradientStop Color="#F8FAFC" Offset="1"/>
                            <!-- almost white -->
                        </LinearGradientBrush>
                    </TextBlock.Foreground>
                    <TextBlock.Effect>
                        <DropShadowEffect BlurRadius="8" 
                                          ShadowDepth="2" 
                                          Opacity="0.3" 
                                          Color="Black"/>
                    </TextBlock.Effect>
                </TextBlock>
            </Grid>

            <!-- Photo Display Area - Centered Single Image -->
            <Grid Grid.Row="1" Margin="30,20,30,20">
                
                <!-- Centered Photo Container -->
                <Border Background="Transparent" 
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Center">
                    
                    <!-- Main Photo Image -->
                    <Image Name="ComposedPhotoImage" 
                           Stretch="Uniform" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Center"
                           MaxWidth="800"
                           MaxHeight="600"
                           RenderOptions.BitmapScalingMode="HighQuality"
                           RenderOptions.EdgeMode="Aliased">
                        <Image.Effect>
                            <DropShadowEffect BlurRadius="15" Opacity="0.4" ShadowDepth="3"/>
                        </Image.Effect>
                    </Image>
                    
                </Border>
                
            </Grid>

            <!-- Template Info -->
            <Grid Grid.Row="2" Margin="30,10,30,10">
                <StackPanel HorizontalAlignment="Center">
                    
                    <!-- Template Info Card -->
                    <Border Background="#4338CA" 
                            CornerRadius="12" 
                            Padding="20,12" 
                            HorizontalAlignment="Center">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="8" Opacity="0.3" ShadowDepth="3"/>
                        </Border.Effect>
                        
                        <StackPanel Orientation="Horizontal">
                            <!-- Sparkle Icon -->
                            <TextBlock Text="✨" 
                                      FontSize="20" 
                                      VerticalAlignment="Center" 
                                      Margin="0,0,12,0"/>
                            
                            <StackPanel>
                                <TextBlock Name="TemplateNameText" 
                                          Text="Template Name" 
                                          FontSize="20" 
                                          FontWeight="Bold" 
                                          Foreground="White" 
                                          HorizontalAlignment="Center"/>
                                <TextBlock Name="PhotoCountText" 
                                          Text="4 Photos • Ready to print" 
                                          FontSize="14" 
                                          Foreground="White" 
                                          HorizontalAlignment="Center" 
                                          Opacity="0.9"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    
                </StackPanel>
            </Grid>

            <!-- Action Buttons - Enhanced Size -->
            <Grid Grid.Row="3" Margin="30,20,30,20">
                <StackPanel Orientation="Horizontal" 
                           HorizontalAlignment="Center">
                    
                    <!-- Retake Photos Button -->
                    <Button Name="RetakeButton" 
                            Content="Retake Photos" 
                            Style="{StaticResource RetakeButtonStyle}" 
                            Width="220"
                            Height="50"
                            FontSize="18"
                            Margin="0,0,25,0"
                            Click="RetakeButton_Click"/>
                    
                    <!-- I Love Them Button -->
                    <Button Name="LoveThemButton" 
                            Style="{StaticResource LoveButtonStyle}" 
                            Width="220"
                            Height="50"
                            FontSize="18"
                            Margin="25,0,0,0"
                            Click="LoveThemButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="I Love Them!" 
                                      VerticalAlignment="Center" 
                                      Margin="0,0,10,0"/>
                            <TextBlock Text="❤️" 
                                      FontSize="18" 
                                      VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- Loading Overlay -->
            <Grid Name="LoadingOverlay" 
                  Background="#80000000" 
                  Visibility="Collapsed">
                <Border Background="White" 
                        CornerRadius="12" 
                        Padding="32,24" 
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <!-- Loading Spinner -->
                        <Border Width="24" 
                               Height="24" 
                               BorderBrush="#667eea" 
                               BorderThickness="3,3,1,1" 
                               CornerRadius="12" 
                               Margin="0,0,16,0">
                            <Border.RenderTransform>
                                <RotateTransform x:Name="LoadingSpinnerTransform"/>
                            </Border.RenderTransform>
                            <Border.Triggers>
                                <EventTrigger RoutedEvent="Loaded">
                                    <BeginStoryboard>
                                        <Storyboard RepeatBehavior="Forever">
                                            <DoubleAnimation Storyboard.TargetName="LoadingSpinnerTransform"
                                                           Storyboard.TargetProperty="Angle"
                                                           From="0" To="360" Duration="0:0:1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Border.Triggers>
                        </Border>
                        
                        <TextBlock Name="LoadingText" 
                                  Text="Composing your photos..." 
                                  FontSize="16" 
                                  FontWeight="Medium" 
                                  Foreground="#374151" 
                                  VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Error Message -->
            <Border Name="ErrorMessageBorder" 
                    Background="#DC2626" 
                    CornerRadius="8" 
                    Padding="16,12" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Top" 
                    Margin="0,200,0,0" 
                    Visibility="Collapsed">
                <TextBlock Name="ErrorMessageText" 
                          Text="Error Message" 
                          Foreground="White" 
                          FontSize="16" 
                          FontWeight="Medium"/>
            </Border>
        </Grid>
    </Grid>
</UserControl> 